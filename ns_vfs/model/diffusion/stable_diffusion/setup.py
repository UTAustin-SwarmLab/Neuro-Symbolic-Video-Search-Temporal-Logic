from setuptools import find_packages, setup

setup(
    name="stable_diffusion",
    version="0.0.1",
    description="",
    packages=find_packages(),
    install_requires=[
        "tqdm",
    ],
)
